import demo from '@Themes/demo.json'
import themes from '@Themes/themes'

import ThemeComponents from '.'
import { PaperMetadata } from '@/lib/types'

export default function Default({
  paperMetadata,
  scale,
}: {
  paperMetadata: PaperMetadata
  scale: number
}) {
  const  = {...themes[theme.extends],...theme}
    return <></>
}